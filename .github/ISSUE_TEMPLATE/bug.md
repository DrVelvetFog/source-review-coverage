---
name: Bug
about: The action or the verifier did something other than what the documentation says.
title: ""
labels: bug
---

**What happened.** The verdict, warning or error, quoted.

**What you expected.** The line of `docs/action.md`, the specification, or the README you
were relying on.

**How to reproduce.**
- Repository and event (`push` to main, `pull_request`, `pull_request_review`):
- Workflow run URL:
- The four artifact files, attached, or the verifier's eight result lines pasted:
- `git --version` where it was recomputed, if it was:

Never paste a token. The artifact is public by construction; the record, the statement
and the bundle contain no secret.
